NEXTAUTH_URL=http://localhost:3000 DATABASE_URL=$(heroku config:get DATABASE_URL -a zen-habits-training) SECRET=$(heroku config:get SECRET -a zen-habits-training) EMAIL_USERNAME=$(heroku config:get EMAIL_USERNAME -a zen-habits-training) EMAIL_PASSWORD=$(heroku config:get EMAIL_PASSWORD -a zen-habits-training) EMAIL_FROM=$(heroku config:get EMAIL_FROM -a zen-habits-training) PGSSLMODE=no-verify yarn dev
